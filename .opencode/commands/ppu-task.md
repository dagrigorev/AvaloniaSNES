---
description: Implement a focused PPU/rendering task
agent: snes-orchestrator
---

Implement this PPU/rendering task:

$ARGUMENTS

Use `@ppu-rendering-agent`.

Scope must stay focused. Prefer one feature/register/rendering rule per change. Add synthetic VRAM/CGRAM/OAM tests in `tests/SnesEmulator.Rendering.Tests`.

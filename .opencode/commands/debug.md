---
description: Investigate a crash, hang, bad ROM behavior or bad rendering
agent: snes-orchestrator
---

Debug this issue:

$ARGUMENTS

Use `@debugger-agent` first unless the subsystem is obvious.

Follow @.opencode/workflows/debugging.md.

Do not leave noisy tracing enabled by default. Convert the reproduced issue into a regression test when possible.
